---
name: update
description: Update workspace scaffolding files (CLAUDE.md, _ctx/CLAUDE.md, templates) to the latest ctx plugin version. Does not touch feature context, repos, or WORKSPACE.md.
---

# Update Workspace

Regenerates workspace scaffolding files from the latest ctx plugin templates. Use this after updating the ctx plugin to get the latest agent instructions, routing rules, and templates.

## What gets updated

- `CLAUDE.md` (workspace root) — agent instructions and skill routing
- `_ctx/CLAUDE.md` — agent instructions for the context directory
- `_ctx/_template/` — feature templates (product.md, engineering.md, implementation.md)
- `_ctx/README.md` — context directory description

## What is NOT touched

- `_ctx/WORKSPACE.md` — workspace map (generated by `ctx:init`)
- `_ctx/index.md` — project index (generated by `ctx:add-index`)
- `_ctx/config.json` — repo config (generated by `ctx:export`)
- `_ctx/{feature}/` — all feature context files
- Any repo content

## Workflow

### 1. Verify workspace

Check that `_ctx/` exists. If it doesn't, tell the user to run `ctx:init` first and stop.

### 2. Load templates

Read the `ctx:init` skill file (`skills/init/SKILL.md` relative to the plugin root) to get the canonical templates. All templates are defined there — this skill does not duplicate them.

### 3. Diff and update each file

For each scaffolding file, compare the current content against the template from init. If there are differences:

- Show the user a summary of what changed (not a raw diff — describe the changes in plain language, e.g. "Added automatic skill routing section to CLAUDE.md")
- Write the updated file

If a file is already up to date, skip it silently.

### 4. Summary

Tell the user which files were updated and which were already current.
